{
  "name": "avam-foundation",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm run dev",
    "server": "cd server && node index.js",
    "build": "cd client && npm run build",
    "start": "cd server && node index.js"
  },
  "dependencies": {
    "concurrently": "^9.2.4"
  }
}
